
  # Sales Landing Page

  This is a code bundle for Sales Landing Page. The original project is available at https://www.figma.com/design/wk68TrmH5MbInIn5NwK4h0/Sales-Landing-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  